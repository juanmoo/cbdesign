# Software completion status

Updated 2026-09-24. Execution follows the approved remaining-software plan; illustrative inputs only. User authorized tested checkpoint pushes to GitHub and no further routine questions during the run.

## Verified baseline

- N1 committed/pushed `a982b94`; 99 tests passed locally.
- GitHub Python 3.12/3.13 CI succeeded: https://github.com/juanmoo/cbdesign/actions/runs/35957916069.

## Working integration (not final milestone acceptance)

- 147 local tests pass at the integrated compiler/search/UI/export checkpoint. Installed-wheel generation and real CLI search pass.
- N2: versioned strict requests, explicit grid trims, rectangular sizing, finite inventory consumption, deterministic exact compilation and independent replay.
- N3: bounded grid/recipe candidates, exact post-trim area scoring and replay-derived metrics; nondominated archive and cancellation.
- Target ingestion: bounded Pillow decoding, frozen binary targets, common patterns and a generated three-circle mouse silhouette.
- F1: digest-bound named affine dimensional propagation, separate from nominal replay; accepted nonzero shared-setting tests and endpoint/material guard regressions.
- CLI: demo, generate, search, validate with optional uncertainty, and serve. Common deterministic PDF/CSV/JSON/SVG bundles; ZIP alternatives.
- F2: loopback browser forms, PNG upload, search progress/cancel, project save/load and ZIP downloads. Actual Chromium mouse PNG upload → four alternatives → nominal-valid ZIP download → PNG project save/reload passed; screenshot inspected. Final visual polish and cancellation/invalid-input smoke remain.
- PDF: reference report rendered and visually inspected; table number wrapping corrected, operation blocks kept together.

## In progress / outstanding gates

- Canonical reversal-family panel batching is integrated and covered by regressions. Real mouse PNG search returns distinct replay-valid alternatives with differing likeness/source/joint/saw metrics. Final measured gallery pending.
- Multi-strip source packing is not implemented; finite stock is currently consumed conservatively one source per cell strip.
- Browser target/difference previews and complete project/upload/download/cancellation smoke.
- Full eight-pattern measured gallery regeneration, exact versus recipe budgets, screenshot/PDF reinspection.
- Installed-wheel smoke, expanded Python 3.12/3.13 remote CI, final documentation and tested checkpoint publication.

## Non-software gates

Physical expert review, fabrication/measurement and safety/joint certification remain outstanding. No generated result is shop-ready merely because nominal or supported dimensional checks pass.
